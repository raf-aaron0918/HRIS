# TODO

- [x] Remove the Employee/Time entry form UI from `src-vue/pages/AttendancePage.vue`.

- [x] Keep only the Time & Attendance summary card and the Recent Attendance Logs table in `AttendancePage.vue`.

- [ ] Clean up related unused state/computed/functions in `AttendancePage.vue` so the page builds.
- [ ] Ensure `/attendance` route still renders correctly.
- [ ] (Optional) Confirm if the “New Attendance” button on `/attendance` should be hidden or kept.

