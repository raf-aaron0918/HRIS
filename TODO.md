# TODO

- [x] Remove/replace the landing page marketing headline text in `src/html/index.html`.
- [x] Remove `landing-page` body class from `src/html/index.html`.
- [ ] Make the default `dist/index.html` route to dashboard page (replace `src/html/index.html` content with dashboard include or redirect).
- [ ] Rebuild with gulp to regenerate `dist/`.
- [ ] Verify opening `dist/index.html` shows the dashboard (not landing page styling/text).

